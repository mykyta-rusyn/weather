const bgColors = {
	transparent: 'transparent',
	lightGreen: 'lightgreen'
};

const textColors = {};

const borderColors = {};

const shadowColors = {};

export const colors = {
	...bgColors,
	...textColors,
	...borderColors,
	...shadowColors,
};
