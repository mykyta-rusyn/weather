const arrayLiteral: WeatherPeriod[] = ['one', 'tree', 'week', 'twoWeek'];
const arrayNumber = [1, 3, 7, 14];

export type WeatherPeriod = 'one' | 'tree' | 'week' | 'twoWeek';
export const periods = {
	arrayLiteral,
	arrayNumber
};
